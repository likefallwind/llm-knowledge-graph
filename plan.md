# AI 全领域知识图谱：最小重写方案

> vNext（2026-08-03）：下面的“六种实体类型/三种关系白名单”和单次片段抽取已由
> 本节覆盖，其余“语料是唯一知识来源、SQLite、小函数、Observation 永久保留”原则继续有效。

## 0. vNext：吸收 KGGen 与 Tree-KG

1. 采用两阶段开放抽取：先在原始 Passage 中抽 Entity 与开放类型标签，再给定实体清单抽开放谓词关系。
2. 原始实体类型和关系谓词永久保存；全局开放词表通过精确别名、轻量候选召回和 LLM 同义裁判归一，宁可重复也不误并。
3. `is_a / part_of / prerequisite_of` 只是种子关系及循环检查类别，不再是关系白名单；有原文明确证据的其他关系同样进入图谱。
4. Markdown/HTML/教材编号标题形成持久化 Section 树；摘要自底向上生成，只能压缩 Passage 与子节摘要。
5. 目录距离只用于关系补抽的候选排序。任何补抽关系仍须引用真实 Passage 并通过独立证据裁判，目录和摘要本身不能作为 Evidence。
6. 定义继续由某个 Entity 的全部 EntityObservation 聚合生成，并保留引用与失败边界。
7. 语义、目录、混合三种视图分别回答“知识关系是什么”“教材在哪里讲”“结构和语义如何对应”。
8. schema 9 使用全新 `data/knowledge-vnext.db`；旧 schema 的抽取语义不同，不做自动迁移或混用。

## 1. 项目目标

构建一个由 AI 持续阅读语料、自动抽取并逐步合并形成的人工智能领域知识图谱，最终覆盖 AI 的主要知识体系，并为知识导航和个性化教学提供基础。

核心闭环：

```text
广泛阅读语料 → 抽取知识 → 对齐实体 → 合并图谱 → 继续阅读
```

图谱不要求一次成型。允许孤立实体、局部子图和暂时重复的实体存在；随着更多语料进入，再逐渐合并和连接。

---

## 2. 原则：尽量简单

1. 因为最终目标是构建整个AI领域知识图谱，这会成为一个很大的图谱，如果各种规则极为复杂，大概率会陷入到各种复杂情况而难以完成
2. 同时目前很多事情不确定情况下，复杂的代码也难以维护

因为本项目不要涉及非常复杂的工程实现，要以学术项目为主，代码容易维护

---

## 3. 最小知识模型

系统只保留四类对象：

| 对象         | 含义                    | 最小内容                    |
| ---------- | --------------------- | ----------------------- |
| `Source`   | 被读取的语料及其版本            | 名称、类型、URI、正文、内容哈希、语言    |
| `Entity`   | 可独立定义或学习的知识对象         | 规范名、别名、定义、类型            |
| `Claim`    | 两个实体之间的规范关系           | subject、relation、object |
| `Evidence` | 支持 Entity 或 Claim 的原文 | 来源、摘录、位置、支持或反对          |

此外保存抽取阶段的 `ClaimObservation` 作为审计与延迟落实记录；它不是第五类
图谱对象，也不参与图谱导出。只要通过 Passage 机械校验，即使端点暂时不是
Entity，Observation 和原文证据也必须保留。

### 3.1 Entity

实体只保留六种主类型：

```text
resource   论文、教材、课程、文档等知识载体
criterion  损失函数、指标、优化目标、评测协议
data       数据集、训练数据、样本集合
task       有目标、输入输出或成功条件的问题
solution   算法、过程、模型、架构、系统或工具
concept    数学对象、性质、规律、现象、研究领域等其他对象
```

类型根据实体定义判断，而不是只看名称；`concept` 是兜底类型。

颗粒度只使用一条规则：

> 一个对象在当前语料中具有可稳定复指的名称，并且原文提供了足以识别其身份或说明其知识含义的实质性描述时，可以成为 Entity。比如“定义”“公式”这些没有特别明确的知识含义，所以不能称为entity

### 3.2 Claim

第一版只保留三种关系：

```text
is_a             A 是 B 的一种
part_of          A 是 B 的真实组成部分或明确阶段
prerequisite_of  理解 A 是学习 B 的实质性前提
```

不设置通用的 `related_to`，也不预先加入大量关系。以后只有当一种关系反复出现、语义边界清楚且确实有用时，才增加。

相同的 `(subject, relation, object)` 只保存一条 Claim；不同来源的 Evidence 追加到同一条 Claim 上。

---

## 4. 主流程：以广泛阅读为主

图谱主要通过持续读取大量高质量语料增长，而不是先让图谱决定所有阅读任务。

优先读取：

1. 教材、课程材料和权威综述；
2. 原始论文和官方技术文档；
3. 数据集、模型和评测的官方说明；
4. Wikipedia 等百科材料。

第一版直接使用人工维护的语料目录批量处理，不建设复杂的覆盖规划器。

每个文本片段交给 LLM，抽取：

```text
Entity：名称、定义、类型、原文证据
Claim：subject、relation、object、原文证据
```

没有原文证据的内容直接忽略。

完整流程：

```text
1. 读取并保存 Source
2. 将正文切成适当片段
3. LLM 抽取 EntityObservation、ClaimObservation 和原文证据
4. 永久保存通过 Passage 校验的 ClaimObservation，并独立完成关系证据裁判
5. 将新 Entity 与已有 Entity 对齐
6. 将 Observation 端点替换为唯一规范 Entity；不能唯一解析时保持 pending
7. 两端齐全且证据支持时，新 Claim 入图；已有 Claim 追加 Evidence
8. 新 Entity、alias 或合并出现后重放 pending Observation，不重新抽取语料
9. 继续读取下一批语料
```

新实体暂时无法连接到旧图不是错误。只要有清楚定义和来源，就可以先保存，等待后续语料补充关系。

Claim 端点暂时不是 Entity 也不是错误。相同端点在至少三个独立 Passage 中
反复出现时进入 Entity 晋升审核；必须能够从这些原文中形成稳定名称和实质性
定义，才能创建 Entity。重叠 Chunk 中的同一 Passage 只计一次。

---

## 5. 实体识别：让 LLM 判断是否为同一对象

实体识别的核心问题是：

> 新名称和已有名称是否指向同一个知识对象？

只保留两步：

1. 根据规范化名称、别名和简单字符串相似度，召回少量候选实体；
2. 给 LLM 双方的名称、定义和上下文，让其判断：

```text
same       同一个实体，合并
new        不同实体，新建
uncertain  信息不足，暂不合并
```

`uncertain` 时直接保留为独立实体，以后遇到更多语料再重新判断。

第一版不设计置信度累乘、多级别名状态或复杂审核队列。

基本原则是：

> 宁可暂时产生重复实体，也不要把不同实体错误合并。

考虑到一个实体可能有很多不同的名字，需要维护一个正式的官方名字（canonical name），同时维护一系列别名（alias）。正式官方的名字不一定是语料直接抽取的，而是抽取的名字经过LLM以后给出的

---

## 6. LLM 的角色边界

LLM 可以用于：

* 从语料中抽取实体和关系；
* 判断两个实体是否相同；
* 判断证据是否支持某条关系；
* 判断实体类型；
* 翻译英文语料；
* 发现疑似重复、异常关系和缺失内容；
* 必要时规划定向搜索。

唯一禁止的是：

> LLM 不能仅凭自身参数记忆，直接给图谱输出实体节点

最简洁的边界是：

> **LLM 负责理解和判断语料，语料负责提供知识，LLM可以做裁判。**

翻译可以帮助抽取，但翻译不是新的独立来源，必须保留原文及来源信息。

---

## 7. 主动搜索只是补充

主动搜索只用于广泛阅读后仍存在的问题，例如：

* 重要实体长期孤立；
* 两个实体疑似相同但信息不足；
* 某条重要关系证据不清；
* 某个重要领域明显缺失；
* 分类或先修结构出现明显断点。

找到补充语料后，仍然走同一套“读取—抽取—对齐—合并”流程，不能绕过语料直接补图。

一句话概括：

> **广泛阅读负责发现知识，主动搜索负责修补图谱。**

---

## 8. 第一版只保留的规则

1. 每个 Entity 至少有一段来源证据；每个 Claim 至少有一段可定位的关系证据。
2. Claim 两端必须对应已存在的 Entity。
3. 不允许自环和完全重复的 Claim。
4. `is_a` 与 `prerequisite_of` 不允许形成明显循环。
5. 类型主要用于描述和检索，不作为大多数关系的硬性拦截条件。
6. 不确定时不强行判断：实体暂不合并，关系暂不写入。
7. 端点未解析、证据不足和 alias 歧义都保留 Observation；只有机械契约无效
   才是终止性拒绝。

除此以外，不提前增加复杂规则。

---

## 9. 第一版明确不做

* 不设计 `proposed`、`published`、`shadow` 等复杂状态机；
* 不把主动搜索作为主流程；
* 不要求每个实体第一次出现时就连接到主图；
* 不提前解决生产级并发、权限和规模化部署。

代码也保持最小：使用 SQLite 加少量 Python 模块，围绕一条流水线实现即可：

```text
Read → Extract → Resolve → Merge → Repeat
```

---

## 10. 第一版完成标准

第一版只需证明六件事：

1. 能批量读取不同来源的语料；
2. 能抽取带原文证据的 Entity 和 Claim；
3. 能用 LLM 判断相似名称是否应当合并；
4. 相同 Claim 能累计多个来源的 Evidence；
5. 允许孤立实体存在，并能随着后续语料逐渐连接；
6. 系统可以持续运行核心闭环，而不是依靠人工逐条维护。

任何新增机制都必须先回答：

> 不加它，当前核心闭环是否真的无法工作？

如果答案是否定的，就暂时不加。
